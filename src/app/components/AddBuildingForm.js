import { Building } from "@influenceth/sdk";

// const construction = building.getConstructionType(building.IDS.EXTRACTOR);

export default function AddBuildingFRorm() {
  // console.log("Building", Building.TYPES    );
  // console.log(
  //   "Object.entries(Building.CONSTRUCTION_TYPES);",
  //   Object.entries(Building.CONSTRUCTION_TYPES)
  // );
  return <div>BBB</div>;
}
